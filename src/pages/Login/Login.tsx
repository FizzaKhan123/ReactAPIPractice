import * as React from "react";
import CountrySelector from "../components/countrySelector/CountrySelector.tsx";
export default function Login() {
  return (
    <div style={{ maxWidth: "400px" }}>
      <CountrySelector />
    </div>
  );
}
